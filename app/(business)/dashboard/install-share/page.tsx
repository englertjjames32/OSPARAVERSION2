import { SectionCard } from "@/components/section-card";

export default function BusinessInstallSharePage() {
  return (
    <SectionCard title="Install & Share">
      <p>Share this public link with customers:</p>
      <code>/b/demo-auto</code>
      <p>Customers can add the page to their phone from the public Add to Phone page.</p>
    </SectionCard>
  );
}
